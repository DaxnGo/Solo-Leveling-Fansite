{
  "name": "solo-leveling-fansite",
  "version": "1.0.0",
  "description": "A responsive and visually appealing website dedicated to the popular manhwa/anime series 'Solo Leveling'",
  "scripts": {
    "build": "chmod +x build.sh && ./build.sh",
    "dev": "serve ."
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
